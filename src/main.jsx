import React from "react";
import ReactDOM from "react-dom/client";
import ClimateMap from "./ClimateMap.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ClimateMap />
  </React.StrictMode>
);
